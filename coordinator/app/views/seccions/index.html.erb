<h1>Listado secciones!</h1>

<table>
  <tr>
    <th>Numero</th>
    <th>Creditos</th>
    <th>Profesor</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @seccions.each do |seccion| %>
  <tr>
    <td><%= seccion.numero %></td>
    <td><%= seccion.creditos %></td>
    <td><%= seccion.profesor %></td>
    <td><%= link_to 'Mostrar', seccion %></td>
    <td><%= link_to 'Editar', edit_seccion_path(seccion) %></td>
    <td><%= link_to 'Eliminar', seccion, method: :delete, data: { confirm: 'está seguro que desea eliminar esta seccion?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nueva Seccion', new_seccion_path %>
